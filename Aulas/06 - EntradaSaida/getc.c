#include <stdio.h>

int main()
{
    char letra; //pega um caracter
    letra = getc(stdin); // le no teclado o caracter
    putc(letra, stdout); // imprime na tela o caracter
    
    
    
    
    printf("\n");
    return 0;
}